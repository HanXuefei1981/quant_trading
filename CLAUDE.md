# 量化交易系统开发原则

## 外部数据质量问题处理原则

**当遇到接口或外部数据（通达信、akshare 等）的质量问题时：**

- **不要**修改程序逻辑来绕过或容忍数据缺失/异常
- **应该**稽核数据的完整性和有效性，然后告知用户具体问题
- 数据质量问题应从数据源头修复，而非在代码层面打补丁

**稽核内容包括：**
- 数据时间范围覆盖是否完整（起止日期）
- 各股票行数是否合理（最近 N 天是否有数据）
- 是否存在异常价格（0值、负值、极端涨跌）
- 关键列（date, open, high, low, close, volume）是否有 NaN

## 项目结构

- Phase 1：数据拉取 + 特征工程（TDX 本地数据 → Parquet 缓存）
- Phase 2：模型训练（LightGBM 三分类）
- Phase 3：回测（多股票组合，每 5 日调仓）

## 已知问题记录

- **2026-05-07 发现**：market_features.parquet 因 `dropna(label)` 导致最后 5 个交易日数据缺失，造成回测末期资产归零（-97%）。根本原因是通达信数据截止日期太早，无法为最后 5 天行计算 5 日后的 label。**处理方式**：用户将重新提取截至 2026-05-07 的完整数据。
